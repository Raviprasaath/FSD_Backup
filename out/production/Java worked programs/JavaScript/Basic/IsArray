function isArray(input){
    let ans = Array.isArray(input);
    if (ans===true) {
        console.log('true');
    } else {
        console.log('false');
    }
}

isArray([1,2])
isArray(1)
isArray('hi')
